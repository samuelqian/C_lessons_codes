gcc -c max.c -o max.o
gcc -c min.c -o min.o
gcc max.o min.o hello.c a.out //max.o min.o is binary file compiled, so it's middle file


head file .h defines what the .o file or the .c file that generates .o file.
